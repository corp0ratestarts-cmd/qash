[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false

# No external dependencies — keep the pure-qash supply chain minimal.
# xtask is a hosted dev tool (Domain B); it may use std freely.
[[bin]]
name = "xtask"
path = "src/main.rs"
