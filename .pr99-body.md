## Summary

Incorporates the actionable PR #93 comments into curated docs instead of the raw transcript branch:

- adds explicit performance target acceptance gates and benchmark-artifact requirements to `ROADMAP.md`
- schedules `Phase 6 — Sharding & Horizontal Scaling` as the production delivery track for deterministic sharding, EFB, ZK batching, cross-shard receipts, and Domain B-only indexing
- schedules the UC-MJA cascade proposal as a research track with accepted/rejected Domain A boundaries
- schedules sovereign storage tiers, `SovereignVault`, and auditable `ShredCommitment` work
- records that LEANN/vector indexing is Domain B-only and cannot drive consensus or sharding
- adds ZK prover sizing methodology requirements before any 10K+ TPS or sub-50ms finality claim
- updates README, roadmap, ADR index, privacy/sharding/hash-cascade specs, and benchmark docs

## Type

- [ ] Proof change (Coq `.v` file)
- [ ] Consensus code — Domain A (`crates/consensus/`, `no_std`, checked arithmetic)
- [x] Spec document update (`docs/spec/`)
- [ ] CI / process / tooling
- [ ] **Genesis constants change** (`GENESIS_CONSTANTS.toml`) ← requires `[genesis-change-acknowledged]` token below
- [ ] Other

## Checklist

### Deduplication
- [x] I searched open **and recently closed** PRs for overlapping work before opening this
- [x] This PR does not duplicate an existing open or merged PR

### Human review
- [ ] A human (not only an AI assistant) has read and understood the changes in this PR
- [x] The changes have been tested or verified locally, not only in CI

### Domain A constraints *(skip if no `crates/consensus/` changes)*
- [x] No `crates/consensus/` changes

### Coq proofs *(skip if no `.v` file changes)*
- [x] No `.v` file changes

### Genesis constants *(skip if `GENESIS_CONSTANTS.toml` not changed)*
- [x] `GENESIS_CONSTANTS.toml` not changed

## Verification

- `bash scripts/check_document_hygiene.sh`
- `git diff --check`
- `git diff --check HEAD~1..HEAD`
- GitHub commit verification: `faf54e3` verified SSH signature
