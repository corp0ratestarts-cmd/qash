fn main() {
  // Minimal contract:
  // 1) read vectors JSON
  // 2) run deterministic transitions
  // 3) emit stable JSON with roots
  // 4) exit nonzero on mismatch
  eprintln!("TBD: implement runner");
  std::process::exit(2);
}
